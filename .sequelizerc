import path, { dirname } from "path"

module.exports = {
  config: path.resolve(__dirname, "src", "config", "database.ts"),
  "migrations-paths": path.resolve(__dirname, "src", "database", "migrations")
}